# رفع حلقهٔ بی‌نهایت صفحهٔ ورود

محتوای این بسته را داخل ریشهٔ پروژهٔ Android کپی کنید و جایگزینی دو فایل
Kotlin را تأیید کنید.

تغییرات:

- صفحهٔ ورود دیگر یک نمونهٔ کامل و تو‌در‌توی `ShetabApp` را به‌عنوان
  پس‌زمینه اجرا نمی‌کند.
- هدایت خودکار به `login_phone` فقط وقتی route فعلی صفحهٔ ورود نباشد انجام
  می‌شود.
- `launchSingleTop` از ساخته‌شدن چند نمونهٔ یکسان صفحهٔ ورود جلوگیری می‌کند.

بعد از کپی، برنامهٔ قبلی را از شبیه‌ساز یا گوشی حذف کنید و در Android Studio
به‌ترتیب **Clean Project** و **Rebuild Project** را اجرا کنید.
