# نسخه کامل و نهایی اندروید شتاب

این بسته شامل کل پروژه اندروید و تمام تغییرات صفحه اصلی، لیگ، اعلان داخل برنامه، گروه، اتصال API و سایر اصلاحات انجام‌شده است.

## تنظیم آدرس سرور

در ریشه پروژه یک فایل با نام `.env` بسازید و مقدار زیر را داخل آن قرار دهید:

```properties
API_BASE_URL=http://10.0.2.2:4001/
```

- برای Emulator اندروید و سروری که روی همان کامپیوتر اجرا می‌شود، معمولاً `10.0.2.2` درست است.
- برای گوشی واقعی، IP شبکه داخلی کامپیوتر را وارد کنید؛ مثلاً `http://192.168.1.10:4001/`.
- بعد از هر تغییر `.env`، پروژه را Gradle Sync و Rebuild کنید.
- وجود `/` در انتهای آدرس اختیاری است.

تمام درخواست‌های بک‌اند، Refresh Token و آدرس فایل‌های نسبی از همین مقدار استفاده می‌کنند. فایل `.env.example` هم نمونه کامل تنظیمات را دارد.

Firebase و FCM فعلاً کامنت و غیرفعال هستند؛ بنابراین نداشتن مقادیر `FIREBASE_API_KEY`، `FIREBASE_APPLICATION_ID`، `FIREBASE_PROJECT_ID` و `FIREBASE_SENDER_ID` باعث خطای BuildConfig نمی‌شود. اعلان‌های داخل خود برنامه همچنان فعال‌اند.

بعد از Extract، پروژه را در Android Studio باز کنید. فایل `local.properties` و مسیر SDK توسط Android Studio ساخته می‌شود. فایل‌های `.env`، `local.properties`، Keystore، Cache و خروجی Build داخل ZIP قرار نگرفته‌اند.
