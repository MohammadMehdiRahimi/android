# Tasks Checklist: Dashboard Cleanup & Visual Refinements

- [x] **1. Presentation Layer (`ReferenceHomeDashboard.kt`)**
  - [x] Remove user nickname/title under user's display name in `HomeTopHeader`.
  - [x] Reduce spacing between `HomeTopHeader` and `PerformanceChartCard`.
  - [x] Remove subtitle text from `FeatureCardLeague`, `FeatureCardStudyGroup`, `FeatureCardExamBuilder`, and `FeatureCardPeerTrouble`.
  - [x] Shift league vector icon further to the left in `FeatureCardLeague`.
  - [x] Further enlarge 3D target image and reduce its distance from top text in `FeatureCardSmartPlan`.

- [x] **2. Verification & Testing**
  - [x] Check assertions in `HomeScreenTest.kt`.
  - [x] Run `compile_applet` to confirm build integrity.
