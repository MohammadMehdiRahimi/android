# Implementation Tasks: Create Custom Study Plan Screen (ایجاد برنامه مطالعاتی)

## فاز ۱: تعریف متون و دارایی‌های بصری
- [ ] افزودن رشته‌های متنی جدید به `res/values/strings.xml`:
  - عنوان: «ایجاد برنامه مطالعاتی»
  - زیرعنوان: «برنامه اختصاصی خود را در چند گام بسازید»
  - سکشن‌ها: «پایه و کتاب»، «فصل و مباحث»، «دوره‌های مطالعه»، «زمان‌بندی»، «خلاصه برنامه شما»
  - دکمه‌ها: «ثبت و ذخیره برنامه»، «استفاده از زمان‌بندی پیش‌فرض»، «مورد انتخاب شده»
- [ ] وکتورهای مورد نیاز (برگ زیست‌شناسی، اتم فیزیک، ارلن شیمی، ماشین‌حساب ریاضی، آیکون فنجان قهوه، آیکون جرقه/جادویی).

## فاز ۲: لایه Domain و ViewModel
- [ ] ایجاد `CreateStudyPlanUiState` با تمام متغیرها، محاسبات زمان کل و انتخاب‌ها.
- [ ] پیاده‌سازی `CreateStudyPlanViewModel` جهت مدیریت کاتالوگ دروس، انتخاب مباحث، تنظیم دوره‌ها و مدت‌زمان‌ها و ارسال تسک‌ها به مخزن/API.
- [ ] تست و اعتبارسنجی محاسبات در ViewModel.

## فاز ۳: پیاده‌سازی کامپوننت‌های رابط کاربری صفحه ایجاد برنامه
- [ ] پیاده‌سازی `CreateStudyPlanScreen.kt` و کامپوننت‌های تخصصی:
  - `CreatePlanTopBar`: دکمه‌های دایره‌ای ناوبری، عنوان و زیرعنوان.
  - `GradeAndBookSection`: دراپ‌داون پایه تحصیلی، کارت‌های افقی دروس با آیکون‌های رنگی و بج انتخاب تیک‌دار سبز.
  - `ChapterAndTopicsSection`: آکاردئون فصل و چیپ‌های انتخابی گفتارها و جمع‌بندی با شمارنده.
  - `StudyPeriodsSection`: کنترلر دوره‌های مطالعه (+ / -) و راهنما.
  - `TimingSection`: سوییچ فعال‌سازی، اسلایدر زمان مطالعه با نشانگرهای پله‌ای و اسلایدر استراحت با آیکون فنجان.
  - `PlanSummaryMatrixCard`: کارت ماتریس ۴ ستونه خلاصه برنامه با محاسبات پویا.
  - `SavePlanButton`: دکمه اصلی بنفش با آیکون ثبت و ذخیره برنامه.

## فاز ۴: ویرایش‌های صفحه برنامه مطالعه (`StudyPlanScreen`)
- [ ] به‌روزرسانی هدر (`StudyPlanTopHeader`) جهت نمایش آواتار واقعی کاربر یا آیکون پیش‌فرض کاربر (`ic_user` / `Icons.Default.Person`) به جای کاراکتر رایا.
- [ ] اطمینان از موقعیت دقیق FAB دکمه '+' در پایین سمت چپ (`Alignment.BottomStart` در RTL).
- [ ] اتصال کلیک روی FAB و دکمه افزودن تسک به ناوبری به صفحه جدید `create_study_plan`.

## فاز ۵: ادغام ناوبری و تست‌ها
- [ ] ثبت مسیر `create_study_plan` در گراف ناوبری (`MainScreen.kt` / `MainActivity.kt`).
- [ ] نگارش تست‌های واحد و Robolectric برای `CreateStudyPlanViewModel` و `CreateStudyPlanScreen`.
- [ ] کامپایل و اعتبارسنجی با `compile_applet`.
