# Tasks Checklist: Focus Timer Count-Up Mode & Arc Controls

- [x] **State & Logic Refactoring:**
  - [x] تبدیل متغیرهای حالت از `remainingSeconds` معکوس به `elapsedSeconds` افزایشی (شروع از 0 ثانیه تا زمان هدف و فراتر از آن).
  - [x] اضافه کردن منطق `isOvertime` و تطبیق داینامیک رنگ حلقه پیشرفت به رنگ سبز (`Color(0xFF10B981)`) هنگام عبور از زمان تعیین‌شده.
  - [x] به‌روزرسانی منطق بازنشانی (Reset) برای برگرداندن مقدار به صفر ثانیه.
  - [x] به‌روزرسانی زمان نمایش داده شده در `TimeTicker` برای نمایش زمان افزایشی (`elapsedSeconds / 60` و `elapsedSeconds % 60`).

- [x] **UI Controls & Crescent Arc:**
  - [x] یکدست کردن استایل دکمه شروع/توقف به دایره (`CircleShape`) هماهنگ با سایر دکمه‌ها.
  - [x] اعمال رنگ‌بندی آبی روشن (`Light Blue / Sky Blue`) برای دکمه شروع و دکمه رد کردن (Skip).
  - [x] تنظیم دقیق فاصله و جابجایی عمودی دکمه‌ها جهت ایجاد فرم هلالی زیبا و ارگونومیک در عرض صفحه با حفظ چینش RTL.

- [x] **Verification & Testing:**
  - [x] اجرای `compile_applet` برای اطمینان از سلامت کامل بیلد و عدم وجود خطای کامپایل.
  - [x] بررسی عملکرد تست‌های مربوطه.
