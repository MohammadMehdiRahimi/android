# Tasks Checklist: Single-Line Card Titles & Enlarged Illustration

- [x] **1. Presentation Layer Refinements (`ReferenceHomeDashboard.kt`)**
  - [x] Enlarge 3D Target & Dart illustration in `FeatureCardSmartPlan` from `130.dp` to `185.dp`+.
  - [x] Make title `"برنامه‌ریز هوشمند شتاب"` single-line in `FeatureCardSmartPlan`.
  - [x] Change `"لیگ‌های\nرقابتی"` to `"لیگ‌های رقابتی"` on a single line and remove narrow column width restriction in `FeatureCardLeague`.
  - [x] Change `"گروه‌های\nمطالعاتی من"` to `"گروه‌های مطالعاتی من"` on a single line in `FeatureCardStudyGroup`.
  - [x] Change `"پرسش از\nهمکلاسی‌ها"` to `"پرسش از همکلاسی‌ها"` on a single line in `FeatureCardPeerTrouble`.

- [x] **2. UI Test Alignments (`HomeScreenTest.kt`)**
  - [x] Update assertions to verify single-line text nodes.

- [x] **3. Verification & Compilation**
  - [x] Run `compile_applet` to ensure zero compilation errors.
